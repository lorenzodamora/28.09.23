using System;
using static System.Console;

namespace _28_09_23
{
	internal class Starter
	{
		/*
		 * Scrivere le classi quadrato e cubo sfruttando l'ereditarietà.
		 * Per ogni classe scrivere:
		 * - attributi
		 * - costruttori
		 * - metodi  get e set
		 * - metodi specifici ( per il quadrato calcolo dell'area e del perimetro, per il cubo superficie totale e volume)
		 * 
		 * !!! inserire provvisoriamente delle stampe all'interno dei costruttori per comprenderne la dinamica
		 */
		static void Main()
		{

		}
	}
}
